変更内容を確認してコミットする。

手順:
1. `git status` と `git diff` で変更内容を確認
2. `git log --oneline -5` で直近のコミットメッセージのスタイルを確認
3. 変更内容に基づいて簡潔な日本語コミットメッセージを作成
4. ユーザーにメッセージを提示して確認を取る
5. 確認が取れたらコミット実行

ルール:
- pushはしない
- .envなど機密ファイルは含めない
- コミットメッセージは日本語で、prefixをつける（fix:, feat:, refactor:, docs:, style: 等）
